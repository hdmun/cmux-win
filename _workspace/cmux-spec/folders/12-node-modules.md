# `node_modules`

## 역할 / 목적

**생성물 전용 디렉터리**. 루트 `package.json`에 선언된 `vercel` 의존성이 설치되는 위치다. `docs-site/`와 `web/`의 의존성은 이 디렉터리에 포함되지 않는다.

## 편집 지침

**완전히 무시**. 절대 직접 수정하지 않는다. 이 폴더는 루트 패키지 재설치 시 재생성되며, 문서 사이트나 웹 앱의 개별 lockfile과는 별개다.

## 불확실성

없음.
